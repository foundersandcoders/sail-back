'use strict'

var curry = require('curry')

module.exports = curry(function object_map (fn, obj) {
  return Object.keys(obj).reduce(function (object, key) {
    object[key] = fn(obj[key])
    return object }, {}) })
