'use strict'

var curry = require('curry')
var handle_error = module.exports = curry((validation_error, body) =>
  body.error === "E_VALIDATION"
   ?  validation_error(Object.keys(body.invalidAttributes))
   :  validation_error(['id']))

