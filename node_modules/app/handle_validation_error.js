'use strict'

var curry = require('curry')
var handle_error = module.exports = curry(function (validation_error, body) {
  return body.error === "E_VALIDATION"
   ?  validation_error(Object.keys(body.invalidAttributes))
   :  validation_error(['id'])} )

