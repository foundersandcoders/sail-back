
function get_payments (Members, id, cb) {
  Members
    .findOne(id)
    .populate('events_booked')
    .populate('membership_type')
    .populate('payments')
    .exec(cb)
}

module.exports = {
  get_payments
}
