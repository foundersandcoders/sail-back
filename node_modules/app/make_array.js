'use strict'

var curry = require('curry')
var concat = require('./concat.js')

module.exports = curry(function make_array (entry, length) {
   if (length === 0) return []
   return concat([entry], make_array(entry, length - 1)) })
