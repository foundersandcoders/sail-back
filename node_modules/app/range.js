'use strict'

var curry = require('curry')
var make_array = require('./make_array.js')(0)

module.exports = curry(function range (start, end) {
  return make_array(end - start).map(function (_, i) { return i + start } ) })
