'use strict'

module.exports = function curry (fn, args) {
  return function () {
    var extended_args = (args || []).concat(require('./arrayify')(arguments))
   return (fn.length - extended_args.length) > 0
        ? curry(fn, extended_args)
        : fn.apply(undefined, extended_args) } }
