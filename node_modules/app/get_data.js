'use strict'

var get = require('./get.js')
var map = require('./map.js')
var prop_or = require('./prop_or.js')

module.exports = function (url) {
  return map(process, get(url)) }

function process (data) {
  return JSON.parse(prop_or('[]', 'body', data)) }

