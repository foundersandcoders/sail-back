'use strict'

var R = require('ramda')
var get = require('./http.js').get

module.exports = function (url) {
  return R.map(process, get(url)) }

function process (data) {
  return JSON.parse(R.propOr('[]', 'body', data)) }

