'use strict'

var curry = require('curry')

module.exports = curry(function (options, props) {
  return options[props.id]
      ? { type: 'select', options: options[props.id] }
      : { type: 'input' } })

