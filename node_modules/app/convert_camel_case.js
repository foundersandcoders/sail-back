import { compose, trim, join, map, split } from 'ramda'

  var add_space_if_upper = c =>
    c.toUpperCase() === c
      ? ' ' + c.toLowerCase()
  	: c

var convert_camel_case = compose(trim, join(''), compose(map(add_space_if_upper), split('')))

module.exports = convert_camel_case
