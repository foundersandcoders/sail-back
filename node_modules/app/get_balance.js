const R = require('ramda')

const pennies_to_pounds = n => n / 100

const get_balance = R.reduce(
  (sum, payment) => payment.category === 'payment' ? (sum - payment.amount) : (sum + payment.amount)
  , 0
)

module.exports = R.pipe(get_balance, pennies_to_pounds)
